# GitHub Action Terminal Access Demo

This repository demonstrates how to access and manage GitHub Actions directly from the terminal using the GitHub CLI (`gh`).

## What you can do with GitHub CLI relating to Actions:

- List workflows: `gh workflow list`
- View workflow status: `gh run list`
- Watch a running workflow: `gh run watch`
- View logs: `gh run view --log`
